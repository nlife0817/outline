# Combined Dockerfile для локальной сборки из исходников.
# Слепок Dockerfile.base + Dockerfile, чтобы можно было собрать одной командой
# и без зависимости от outlinewiki/outline-base в Docker Hub.
# Подключается из local-stack/docker-compose.override.yml.

ARG APP_PATH=/opt/outline

# ---------- Stage 1: build из исходников ----------
FROM node:24.15.0 AS deps

ARG APP_PATH
WORKDIR $APP_PATH

# Сначала копируем только манифесты — кэшируем установку зависимостей
COPY ./package.json ./yarn.lock ./.yarnrc.yml ./
COPY ./patches ./patches

RUN apt-get update && apt-get install -y cmake && rm -rf /var/lib/apt/lists/*
ENV NODE_OPTIONS="--max-old-space-size=24000"

RUN corepack enable
RUN yarn install --immutable --network-timeout 1000000 && yarn cache clean

# Копируем код и собираем
COPY . .
ARG CDN_URL
RUN yarn build

# Удаляем dev-зависимости из node_modules
RUN yarn workspaces focus --production && yarn cache clean

ENV PORT=3000

# ---------- Stage 2: runtime ----------
FROM node:24.15.0-slim AS runner

LABEL org.opencontainers.image.source="https://github.com/outline/outline"
LABEL com.outline.fork="local"

ARG APP_PATH
WORKDIR $APP_PATH
ENV NODE_ENV=production

RUN addgroup --gid 1001 nodejs && \
    adduser --uid 1001 --ingroup nodejs nodejs && \
    mkdir -p /var/lib/outline && \
    chown -R nodejs:nodejs /var/lib/outline && \
    chown -R nodejs:nodejs $APP_PATH

COPY --from=deps --chown=nodejs:nodejs $APP_PATH/build ./build
COPY --from=deps --chown=nodejs:nodejs $APP_PATH/server ./server
COPY --from=deps --chown=nodejs:nodejs $APP_PATH/public ./public
COPY --from=deps --chown=nodejs:nodejs $APP_PATH/.sequelizerc ./.sequelizerc
COPY --from=deps --chown=nodejs:nodejs $APP_PATH/node_modules ./node_modules
COPY --from=deps --chown=nodejs:nodejs $APP_PATH/package.json ./package.json

RUN apt-get update && apt-get install -y wget && rm -rf /var/lib/apt/lists/*

ENV FILE_STORAGE_LOCAL_ROOT_DIR=/var/lib/outline/data
RUN mkdir -p "$FILE_STORAGE_LOCAL_ROOT_DIR" && \
    chown -R nodejs:nodejs "$FILE_STORAGE_LOCAL_ROOT_DIR" && \
    chmod 1777 "$FILE_STORAGE_LOCAL_ROOT_DIR"

VOLUME /var/lib/outline/data

USER nodejs

HEALTHCHECK --interval=1m CMD wget -qO- "http://localhost:${PORT:-3000}/_health" | grep -q "OK" || exit 1

EXPOSE 3000
CMD ["node", "build/server/index.js"]
